# Session 2026-06-24 — handoff / context cache

Compaction point: this thread got huge (slow claude-code turns — see Latency below).
Start a FRESH thread and read this file to rehydrate cheaply. Also read `git log --oneline -12`
and `docs/ISSUES.md`.

## Shipped this session (committed)
- Panoptes PWA icon recolored orange→indigo/violet (de-demonized).
- Per-model **accent color** in the UI (theme B): each model owns a color; `MODEL_ACCENT`/`ACCENTS`
  in app.js. claude-code=orange, qwen3-next-80b=violet, gemma4-26b=comfy pink, bonsai-8b=blue, etc.
- **Image attach → claude-code** (vision): `_image_block` in claude_code.py; server forwards `attachments`.
- **Vision gating**: only vision-capable models get images (`VISION_MODELS`/`_is_vision`); `/argus/models`
  exposes per-model `vision` flag. (z-engineer still in chat selector — ISSUES.)
- **Warm-on-select + buzz**: slow-load models warm on UI select + phone buzz (HA notify) + UI pill.
  `WARM_ON_SELECT={qwen3-next-80b}`, `/argus/warm`, `model_warm` SSE. HA notify path VERIFIED working.
- **Token-budgeted history**: `model_history(..., max_tokens)` trims newest-fit to the model's window;
  server `CONTEXT_WINDOW`/`HISTORY_RESERVE`/`_history_budget`. 80B=8192 window. claude-code exempt.
- Audiobook: grabbed **Viridian Gate Online: Empirical Endgame** (was missing). ABB search lane is DOWN
  (homepage fallback), so pulled it from the existing "Full Works" collection torrent in qBit (deselected
  file → set prio 1 → downloaded 882MB) and copied into the ABS library as Book 8 → scanned, live in ABS.

## UNCOMMITTED (in working tree) — decide before committing
- **Bubble colors** (app.js, index.html styles v37/app v36, styles.css): each assistant bubble's side-bars
  colored by the producing model (per-bubble, snapshot at send); USER bubbles → grey. Approved by Shane,
  just not committed. Live on hard-refresh.
- **docs/ISSUES.md** edits (ABB-lane-down, image-attach follow-ups, 80B-resolved).

## GLM eval — FULLY VALIDATED, ready to A/B
- Candidate vs the 80B / Qwen3.6-35B-A3B. Chose GLM-4.7-Flash (Zhipu, 30B MoE ~3B active, reasoning,
  agentic). Wired into `~/llama-swap/config.yaml` as `glm-4.7-flash`: `GLM-4.7-Flash-UD-Q4_K_XL.gguf`
  (17GB), `-ncmoe 16 -c 16384`, q4 KV, `--temp 0.7 --top-p 1.0 --min-p 0.01`, ttl 600.
- Fits 16GB (~12.9GB used, ~3GB free — could reduce -ncmoe for speed). Loads ~30-60s cold.
- VERIFIED on THIS build (custom PEG-fork llama.cpp, b9415): chat ✓, reasoning (`reasoning_content`) ✓,
  tool calls direct ✓, **complex tool args intact (write_file w/ quotes+newlines → valid JSON)** ✓,
  multi-step tool turn through Argus ✓ (weather-compare, called tool twice + reasoned). Tool-use worry
  was UNFOUNDED on this build. Currently UNLOADED.
- Next: real coding bake-off GLM vs 80B; maybe give GLM an accent + warm_on_select. Net read: GLM can
  *reason* (the 80B is instruct-only) → likely better for coding; ~5x SWE-bench tier vs 80B.

## Latency finding (the "slow replies" investigation)
- NOT swap (red herring — cold pages, low load). Cause = **this conversation's huge context** reprocessed
  every claude-code turn; worse after >5min gaps (prompt cache TTL). Proof: fresh-thread "hi" = **5.1s**
  vs 10–64s here. Fix = fresh thread. Box is fine (anvil 30GB RAM, 16GB 5080). Keep big local models
  unloaded when on claude-code to avoid RAM pressure.

## GLM bake-off + reasoning toggle (2026-06-24, later) — RESOLVED
- **Tool-call path verified on build 9425** (not "no PEG handler" as the old config comment
  feared): template carries `<arg_key>`/`<arg_value>` -> native `Chat format: GLM 4.5`; live
  `tools=` calls return structured `tool_calls`; `write_file` arg with quotes+newlines round-
  trips as valid JSON. **#19009 corruption regression does NOT manifest here.** GLM safe for
  tool write-access in the loop.
- **Bake-off vs 80B:** Round 1 (easy algos: merge-intervals, palindrome-fix, longest-substring,
  flatten-dict) = **tie, both 19/19** — too easy to separate. Round 2 (a REAL `argus/loop.py`
  patch) **80B won decisively**: it shipped a working diff (~2.4k tok, no reasoning); GLM with
  thinking ON *ran away* — 25k chars of reasoning, hit the token cap, emitted **nothing** (twice,
  at 2k and 6k caps). GLM with thinking OFF produced a diff but it was **broken** (referenced a
  param it never added to the signatures). Net: **reasoning HURT here**; 80B is the safer agentic
  coder. GLM's niche = snappy no-think chat.
- **Reasoning toggle WIRED** in `argus/loop.py`: new `enable_thinking: bool|None` param on
  `run`/`stream_run`/`run_async` -> `_thinking_settings()` -> `OpenAIChatModelSettings(extra_body=
  {"chat_template_kwargs":{"enable_thinking":...}})`. None=unchanged. Verified end-to-end through
  Pydantic AI -> llama-swap: False=**0.5s**, default/True=3.1s, all correct. `reasoning_effort` is
  a **no-op** for GLM (don't use it); `--reasoning-format none` is the WRONG knob (would inline
  `<think>` into content). Prod Argus hits llama-swap directly (`ARGUS_MODEL_URL=:9090`), not
  LiteLLM `:4000`, so extra_body passes through clean. **UNCOMMITTED** — `argus/loop.py` edit.
- Config comment on `glm-4.7-flash` updated with this verdict. Hermes-default comment scrubbed.
- **Not yet done:** nothing *calls* `enable_thinking` yet — the param exists but no caller sets
  it. Next: have the server pass `enable_thinking=False` for GLM on simple/chat turns (gate on
  model + task), so chat gets GLM's 0.5s path while agentic turns keep thinking (or just use 80B).

## Open decisions for next thread
1. Commit the bubble-color change + ISSUES edits?
2. ~~GLM coding bake-off~~ DONE (above): 80B is the agentic coder; GLM = fast no-think chat alt.
   Decide: keep GLM around as the snappy chat model, or drop for Qwen3.6-35B-A3B?
3. Give GLM an accent + warm_on_select if keeping it.
4. ABB search lane is down — fix scraper or leave (collection-torrent fallback worked).
5. Commit the `enable_thinking` wiring in `argus/loop.py`? And wire a caller (server gates
   no-think for GLM chat turns).
