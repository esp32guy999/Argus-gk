# Argus session log — 2026-06-20

A long build session. Everything below was built, tested, committed, and verified live.
You (Claude Code in Forge) run a **persistent per-conversation session** in `~/argus` on
**anvil**, with homelab context from `~/argus/CLAUDE.md`. This file is your catch-up on
what changed today. Commits this session: `15571a0` → `fa4b9a3` (24).

## Tool lanes added (Argus reaches ~54 tools now)
- **Shell/CLI lane** (`argus/tools/shell.py`) — guarded `run_command`: allow/deny lists,
  shell vs argv mode, timeout/output-cap/fixed-cwd. (Spec originally banned a shell lane;
  added by owner decision.)
- **Web lane** (`argus/tools/web.py`) — `web_search` (Brave + DuckDuckGo fallback) and
  `web_fetch` (text browser; trafilatura readability + stdlib fallback). Closed the
  "research" gap.
- **Notes** (`argus/tools/notes.py`) — `save_note` writes to `~/argus/notes/`, indexed by
  memory + added to the live index (recall via `lookup_memory`).
- **Navidrome** (`argus/tools/navidrome.py`) — playlists + genre "DJ" (Subsonic salt+token).
- **\*arr acquisition** (`argus/tools/arr_acquire.py`) — `radarr_add_movie` /
  `sonarr_add_series` / `lidarr_add_artist` / `readarr_add_author`, ONE parameterized module
  (SPECS table). Refinable: optional `monitor` (e.g. `latestSeason`) + `quality`. Fixed the
  bug where the model claimed "got Cyndi Lauper" but the lane was read-only.
- **Prowlarr** — read-side OpenAPI lane (status, indexers, search) + a `limits` feature
  capping noisy list endpoints.
- **Audiobooks** (`argus/tools/audiobook.py`) — `audiobook_search` / `audiobook_get` scrape
  **AudiobookBay directly from anvil** (the only host whose VPN reaches ABB; nyx is
  ISP-blocked) → magnet → qBittorrent. Politeness throttle (ABB serves its homepage when
  hammered; results filtered to query matches). Plus a Forge **audiobook widget** (browse/
  search/cover-grid/one-tap grab).

## Harness / server
- **Conversation memory** (`argus/storage.py`) — SQLite (WAL); history threaded into the
  loop so models have memory. Backs `/argus/history` + the conversations UI.
- **Memory Phase 1A** (`argus/memory.py`) — `lookup_memory` over an in-memory embedded
  index of the curated docs (retrieval-first; design in `docs/memory-design.md`).
- **Background task runner** (`argus/tasks.py`) — `start_background_task` runs long jobs
  detached + pushes an iPhone notification (via **Home Assistant directly**, not the old
  :8095 app).
- **System prompt** — now states it runs synchronously (no background/continuation) so it
  ACTS instead of promising work it can't do.
- **Status pill** — replaced the rAF canvas with a live pill (elapsed timer + stall/crash
  + real tool/loop events). Mobile: panels stack vertically (no floating drag canvas).
- **Claude Code as a Forge model** (YOU) — `argus/claude_code.py`: persistent per-conversation
  `claude` stream-json subprocess, resumable, bypasses llama-swap (no GPU touched). Mined
  from the `peace` app (env isolation, cred sync, 64MB readline, session-id resume).

## Hard-won gotchas (don't relearn these)
- **No Hermes code** — inherit lessons, not code (see `[[feedback_no_hermes_code]]`). Forge
  fork is fine; peace/audiobook-getter are Shane's own (not Hermes).
- **ABB only reachable from anvil's VPN** (`tun0`); nyx times out. ABB's RSS search ignores
  `?s=`; its HTML search results are in `div#content` (sidebar contaminates otherwise).
- **Claude Code won't expand `@import`s outside the workspace tree** → homelab facts are
  inlined in `~/argus/CLAUDE.md`; Read `/home/shane/tools.md` for full detail.
- **Navidrome / qBittorrent password is `[redacted — in gitignored config]`** (lowercase). qBt login needs a
  `Referer` header (CSRF) and returns 204 (not 200) on success.
- **64KiB readline trap** bit both the claude-shim and audiobook-getter — raise the
  subprocess `limit`.
- `claude` binary is at `~/.npm-global/bin/claude` (NOT `~/.local/bin`).
- The 80B idle-unloads via llama-swap's own policy; unrelated to the CC path.

## Build method
Local **Qwen3-80B drafts** pattern-following lanes → contract test validates → Claude
reviews (the drafting loop). Each lane has a `tests/test_*.py` (run standalone). Full
suite is 14 files, all green.

## Open / next
Promotion wizard (consent widget), OTel spans, segfault-at-teardown, item-level
acquisition targeting, peace-style upgrades. See `[[project_argus_assistant]]`.
