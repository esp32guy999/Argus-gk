# Argus session log — 2026-06-21

Continues `session-2026-06-20.md`. Theme of the day: **make it real and keep it honest**
— polish the Forge UX, back up the repo, and untangle the homelab's secret sprawl.
Commits: `dab23b2` → (this). Read with `git log --oneline -40`.

## Forge UX
- **Status pill → tap-to-expand activity stream** (built by the Forge Claude Code itself,
  reviewed here): the pill stays the always-on health gauge; tapping it (claude-code path)
  unfolds thinking / `tool_use` / `tool_result`, with per-turn **cost + duration** on the
  bubble. Backend emits `bubble_activity` SSE; `_parse_event()` is a pure, tested helper.
- **Right-swipe = quote-reply** (selection-first, whole-bubble fallback). Left-swipe still
  deletes; long-press left as native select/copy. Removed the redundant corner-× delete.
- **Weather lane + widget** (Open-Meteo, keyless), **Claude re-login widget** (drive
  `claude auth login` via PTY from the phone), **lidarr_get_album** (album-level acquire) —
  all built by the Forge CC, committed per-feature.

## Git backup
- Argus was **local-only (51 commits, no remote)**. Now pushed to **private GitHub
  `esp32guy999/argus`** (auth via `~/.git-credentials`). A pre-push secret scan caught a
  leaked password I'd put in the 0620 session log → scrubbed from history with
  `filter-branch` before the first push. **Rule: scan tracked files for secrets before push.**

## Secrets consolidation (big one — full record in `~/secrets-inventory.md`)
- Swept anvil + nyx + glassgarden → inventory map (no values, mode 600, local-only).
- **Canonical = `nyx:~/.config/secrets/credentials.env`**; clean replica on anvil. Every
  anvil consumer now reads it: argus-ui via `EnvironmentFile=`, mcpo + litellm via
  symlink-to-canonical. No divergent secret copies on anvil.
- **Leak fixed:** stripped the live HA token + all API keys out of tools.md /
  morning-briefing.md / MEMORY.md → pointers to credentials.env.
- Scrubbed dead cruft (archived first): central-brain token, atrium service+secrets, 303M
  dead-readarr instance, stale backups/envs, freya refs.
- **Remaining:** rotation phase (revoke the 2nd HA token; rotate the reused `freedom8824`) —
  needs Shane (touches Amperfy/ABS/HA clients). Soak.

## Gotchas learned today
- **`localhost` in a shared/replicated secrets file is a footgun** — `HA_URL=localhost`
  was right on nyx, broke on anvil (→ a 502). Use the Tailscale hostname so values travel.
- **hermes-gateway is ACTIVE** (Forge chat path), not as retired as notes implied — kept
  `.hermes/.env`. Check consumers before deleting anything.
- **Readarr at :8787 is the `bookshelf` container** (key `782c`); the `readarr` appdata dir
  was a dead duplicate (`88b4`, no container) — removed.
- One password (`freedom8824`) is reused across qBt / Audiobookshelf / Navidrome.
- Two valid HA long-lived tokens exist; consolidate to one, revoke the other.

## Onboarding / docs
- Dogfood test: a fresh `claude -p` was ~80% productive from SPEC.md + CLAUDE.md but had
  to dig out (a) the lane-adding mechanics and (b) how to run tests. Fixed with
  **`docs/CONTRIBUTING.md`** (the dev loop) + a doc-coverage test + a pre-commit nudge so
  doc drift is *enforced*, not just requested.

## Open / next
Rotation phase (Shane); SPEC.md refresh (lists pre-today lanes); the promotion wizard;
OTel spans. See `[[project_argus_assistant]]`.
