# Session 2026-08-19 — wire Qwen 3.8 27B into Argus

**Anchor:** add the already-downloaded `qwen3.8-27b` GGUF to llama-swap and the Argus
model dropdown, configured so chat + tools actually work. Success: it appears in
`/argus/models`, a no-think completion returns content, a tool call is valid JSON.
Out of scope: vision mmproj, MTP/spec-decode, making it the default, granting shell.

## What shipped

- `~/llama-swap/config.yaml` entry `qwen3.8-27b` — UD-Q3_K_XL, `-c 32768`, q4 KV,
  `--reasoning off`, instruct sampling. watch-config reloaded it.
- `config/models.yaml` — display `Qwen 3.8 27B`, green accent, `thinking: false`
  (template defaults to xhigh thinking if omitted), `warm: true`, context 32768.
- `docs/models/qwen3.8-27b.md` + lineup row in `docs/models/README.md`.
- `tests/test_model_config.py` assertions for the new manifest block.

## Verified

- `/v1/models` and `/argus/models` list `qwen3.8-27b`.
- llama-swap chat: `PONG` in 10.6s cold, empty `reasoning_content`.
- Native tool call: `get_time` with `arguments: {}`.
- Live `/argus/chat` harness path: `PONG`.
- VRAM 14659/16303 MiB (~1.1GB free) at 32k ctx.

## Not done

- No mmproj → not marked vision.
- MTP left off (Loki crash trap).
- `deploy/llama-swap-config.yaml` is already stale vs live; not synced.
