# Session 2026-08-20 — hygiene: one writer per fact

**Anchor:** land the working tree, retire SS as a product, stop loop-retry
crashes, make docs match. Success: tests green, SEE has no `ss_shadow`,
chat inbox + personas in git, ISSUES/chat-client/ss docs true.
Out of scope: TTS, Needle, reflections loop, giving SEE extra sensors,
repo-wide duplicate safari.

## What shipped

- PWA inbox (`ui/static/chat.js`) + GK/dropdown send path + persona picker.
- Personas: `argus/persona.py`, `personas/loki.md`, any voice on any model.
  `soul.d/` stays capability-only.
- SS shadow hook removed from `argus/see/api.py`. Sensor pulses removed from
  `loop.py` / `ui/server.py`. `ARGUS_SS=0`. `specs/ss.md` retired.
- `UnexpectedModelBehavior` (tool max_retries) returns a sentence, not a crash.
- Docs: `specs/chat-client.md`, `ISSUES.md` (loop crash + z-engineer),
  `DESIGN-reflections-loop.md` historical, `CLAUDE.md`.

## Not done

- `argus/ss/` files still on disk, uncalled. Delete later if they stay cold.
- Grok turns can still ghost the last bubble (agent wrap-up, not the PWA healer).
- Default conversation is still a junk drawer — operational, not a code fix.
